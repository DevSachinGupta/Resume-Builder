// import { selectAuthenticateDomain } from '../selectors';

describe('selectAuthenticateDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
