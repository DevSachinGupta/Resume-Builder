/*
 * Authenticate Messages
 *
 * This contains all the text for the Authenticate container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Authenticate';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Authenticate container!',
  },
});
