/*
 * CheerioComponent Messages
 *
 * This contains all the text for the CheerioComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CheerioComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CheerioComponent component!',
  },
});
