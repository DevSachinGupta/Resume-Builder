/**
 *
 * FeedbackForm
 *
 */

import React, { memo } from 'react';
// import PropTypes from 'prop-types';
import './style.scss';

function FeedbackForm() {
  return (
    <div>
      <h5>This is FeedbackForm Page</h5>
    </div>
  );
}

FeedbackForm.propTypes = {};

export default memo(FeedbackForm);
