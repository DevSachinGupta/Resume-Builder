/**
 *
 * PublishForm
 *
 */

import React, { memo } from 'react';
// import PropTypes from 'prop-types';
import './style.scss';

function PublishForm() {
  return (
    <div>
      <h5>This is Publish Page</h5>
    </div>
  );
}

PublishForm.propTypes = {};

export default memo(PublishForm);
